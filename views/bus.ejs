<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta http-equiv="x-ua-compatible" content="ie=edge" />
    <title>Subway</title>


    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.2/css/all.css" />
    <!-- Google Fonts Roboto -->
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700;900&display=swap" />
    <!-- leaflet -->
   
    <!-- bootstrap -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

    <link rel="stylesheet" href="/main.css">
    <style>
        body {
            margin: 0;
            padding: 0;
        }

        #map {
            width: 100%;
            height: 60vh;
        }
    </style>
</head>

<body class="routepage">
    <div class="routesCard">

        <nav class="navbar  d-flex justify-content-center p-3" role="navigation">
            <ul class="d-flex flex-row justify-content-around p-0">
                <li>
                    <a href="/profile"><i class="fas fa-home nav-link"></i></a>
                </li>
                <li class="vehicles"><a class="btn m-2" href="/subway">Subway</a></li>
                <li class="vehicles"><a class="btn m-2" href="/commuterRail">Commuter Rail</a></li>
                
                <li>
                    <a href="/logout"><i class="fas fa-sign-out-alt nav-link"></i></a>
                </li>
            </ul>
        </nav>

        <section class="mt-5">
            <div>
                <h2 class="display-4 text-light">Silver Line</h2>
                <ul class="d-flex  flex-wrap">
                    <%for (let route of busroutes[0]) { %>
                        <li class="vehicles">
                            <a href="/route/<%=route%>" type="button" class="btn btn-lg m-1">
                                <%=route%>
                            </a>
                            <% if(favorites.includes(route)) {%>
                                <i class="fas fa-heart heart"></i>
                                <%} else{%>
                                    <i class="far fa-heart unHeart"></i>
                                    <%}%>
                        </li>
                        <% } %>
                </ul>
    
            </div>
            <div>
                <h2 class="display-4 text-light">Cross Town</h2>
                <ul class="d-flex  flex-wrap">
                    <%for (let route of busroutes[1]) { %>
                        <li class="vehicles">
                            <a href="/route/<%=route%>" type="button" class="btn btn-lg m-1">
                                <%=route%>
                            </a>
                            <% if(favorites.includes(route)) {%>
                                <i class="fas fa-heart heart"></i>
                                <%} else{%>
                                    <i class="far fa-heart unHeart"></i>
                                    <%}%>
                        </li>
                        <% } %>
                </ul>
            </div>
            <div>
                <h2 class="display-4 text-light">1 - 49</h2>
                <ul class="d-flex  flex-wrap">
                    <%for (let route of busroutes[2]) { %>
                        <li class="vehicles">
                            <a href="/route/<%=route%>" type="button" class="btn btn-lg m-1">
                                <%=route%>
                            </a>
                            <% if(favorites.includes(route)) {%>
                                <i class="fas fa-heart heart"></i>
                                <%} else{%>
                                    <i class="far fa-heart unHeart"></i>
                                    <%}%>
                        </li>
                        <% } %>
                </ul>
            </div>
            <div>
                <h2 class="display-4 text-light">50 - 99</h2>
                <ul class="d-flex  flex-wrap">
                    <%for (let route of busroutes[3]) { %>
                        <li class="vehicles">
                            <a href="/route/<%=route%>" type="button" class="btn btn-lg m-1">
                                <%=route%>
                            </a>
                            <% if(favorites.includes(route)) {%>
                                <i class="fas fa-heart heart"></i>
                                <%} else{%>
                                    <i class="far fa-heart unHeart"></i>
                                    <%}%>
                        </li>
                        <% } %>
                </ul>
            </div>
            <div>
                <h2 class="display-4 text-light">100s</h2>
                <ul class="d-flex  flex-wrap">
                    <%for (let route of busroutes[4]) { %>
                        <li class="vehicles">
                            <a href="/route/<%=route%>" type="button" class="btn btn-lg m-1">
                                <%=route%>
                            </a>
                            <% if(favorites.includes(route)) {%>
                                <i class="fas fa-heart heart"></i>
                                <%} else{%>
                                    <i class="far fa-heart unHeart"></i>
                                    <%}%>
                        </li>
                        <% } %>
                </ul>
            </div>
            <div>
                <h2 class="display-4 text-light">200s</h2>
                <ul class="d-flex  flex-wrap">
                    <%for (let route of busroutes[5]) { %>
                        <li class="vehicles">
                            <a href="/route/<%=route%>" type="button" class="btn btn-lg m-1">
                                <%=route%>
                            </a>
                            <% if(favorites.includes(route)) {%>
                                <i class="fas fa-heart heart"></i>
                                <%} else{%>
                                    <i class="far fa-heart unHeart"></i>
                                    <%}%>
                        </li>
                        <% } %>
                </ul>
            </div>
            <div>
                <h2 class="display-4 text-light">300s</h2>
                <ul class="d-flex  flex-wrap">
                    <%for (let route of busroutes[6]) { %>
                        <li class="vehicles">
                            <a href="/route/<%=route%>" type="button" class="btn btn-lg m-1">
                                <%=route%>
                            </a>
                            <% if(favorites.includes(route)) {%>
                                <i class="fas fa-heart heart"></i>
                                <%} else{%>
                                    <i class="far fa-heart unHeart"></i>
                                    <%}%>
                        </li>
                        <% } %>
                </ul>
            </div>
            <div>
                <h2 class="display-4 text-light">400s</h2>
                <ul class="d-flex  flex-wrap">
                    <%for (let route of busroutes[7]) { %>
                        <li class="vehicles">
                            <a href="/route/<%=route%>" type="button" class="btn btn-lg m-1">
                                <%=route%>
                            </a>
                            <% if(favorites.includes(route)) {%>
                                <i class="fas fa-heart heart"></i>
                                <%} else{%>
                                    <i class="far fa-heart unHeart"></i>
                                    <%}%>
                        </li>
                        <% } %>
                </ul>
            </div>
            <div>
                <h2 class="display-4 text-light">500s</h2>
                <ul class="d-flex  flex-wrap">
                    <%for (let route of busroutes[8]) { %>
                        <li class="vehicles">
                            <a href="/route/<%=route%>" type="button" class="btn btn-lg m-1">
                                <%=route%>
                            </a>
                            <% if(favorites.includes(route)) {%>
                                <i class="fas fa-heart heart"></i>
                                <%} else{%>
                                    <i class="far fa-heart unHeart"></i>
                                    <%}%>
                        </li>
                        <% } %>
                </ul>
            </div>
            <div>
                <h2 class="display-4 text-light">700s</h2>
                <ul class="d-flex  flex-wrap">
                    <%for (let route of busroutes[9]) { %>
                        <li class="vehicles">
                            <a href="/route/<%=route%>" type="button" class="btn btn-lg m-1">
                                <%=route%>
                            </a>
                            <% if(favorites.includes(route)) {%>
                                <i class="fas fa-heart heart"></i>
                                <%} else{%>
                                    <i class="far fa-heart unHeart"></i>
                                    <%}%>
                        </li>
                        <% } %>
                </ul>
            </div>
        </section>
    </div>

    <!-- Custom scripts -->

    <!-- bootstrap -->
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"
        integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN"
        crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"
        integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q"
        crossorigin="anonymous"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"
        integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl"
        crossorigin="anonymous"></script>
    <script src="main.js"></script>
    <script src="weather.js"></script>

</body>

</html>